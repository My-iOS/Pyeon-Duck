//
//  ExpirationDateVC.swift
//  Pyeon-Duck
//
//  Created by 준우의 MacBook 16 on 12/13/23.
//

import UIKit

/*
 탭바 컨트롤러 필요사항
 - 유통기한(ExpirationDateVC)
    - 년도는 유저가 생성
    - 월은 1 ~ 12월까지 자동 생성
 - 재고 조사(StockVC)
    - 재고는 체크
    - To-Do 리스트처럼 재고 체크
    - 해당 재고 누르면, 상세 페이지에서 세부 내용 표시
    - 재고 체크 시, 줄 -- 그어지며 흐릿하게 표시
 - 시급 계산(SalaryVC)
    - 주급 / 월급 / 연봉 계산
 - 설정(SettingVC) - (선택 구현)
    - 알림 기능 On/Off(선택구현)
    - 배경/글자 색 변경
    - 글자 크기 변경
 */

class ExpirationDateVC: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
    }
}
